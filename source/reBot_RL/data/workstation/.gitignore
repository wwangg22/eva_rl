# The gaussian desk (356 k splats, NuRec) used to live here as an absolute symlink into the
# Re3Sim repo, which gave everyone who cloned this a dangling link and a placeholder table.
# As of 2026-08-10 the real files are committed through Git LFS (`.gitattributes` already
# tracks *.usd), so the scene renders from a clean clone. Do not re-add them here.

# Superseded photogrammetry assets, kept on disk for A/B against the authored cube only.
objects_object2_backup/
